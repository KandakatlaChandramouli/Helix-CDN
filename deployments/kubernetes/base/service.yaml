apiVersion: v1
kind: Service
metadata:
  name: helix-edge
  namespace: helix

spec:
  selector:
    app: helix-edge

  ports:
    - name: runtime
      protocol: TCP
      port: 9000
      targetPort: 9000

    - name: metrics
      protocol: TCP
      port: 18080
      targetPort: 18080
